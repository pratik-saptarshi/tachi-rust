# Tachi-Rust Run Report: pratik-saptarshi/rocinante

Generated: 2026-06-22T02:27:00Z  
Target: https://github.com/pratik-saptarshi/rocinante  
Workspace: `/private/tmp/rocinante`

## Run Summary
- Scope executed: local clone + security-minded code review against Rust backend and Tauri/React front-end.
- Branch/commit inspected: `main` @ `29a4c68`
- Command run: `cargo test --manifest-path /private/tmp/rocinante/src-tauri/Cargo.toml`
- Result: `test result: ok. 83 passed; 0 failed`

## High-level Findings

1. High — default token secret fallback enables predictable admin token behavior
- Evidence:
  - `/private/tmp/rocinante/src-tauri/src/auth.rs:26-28` uses
    `RUNICIPAL_TOKEN_SECRET` fallback to `"dev-secret-key"` in production code.
  - Token issuance/verification in the same file (decode/sign/require_admin) depends on this value.
- Impact:
  - If environment variable is unset in deployment, admin tokens can be forged.
  - This is a local privilege escalation channel for any code path accepting admin JWTs.

2. Medium — unauthenticated scan command allows arbitrary repository traversal
- Evidence:
  - `/private/tmp/rocinante/src-tauri/src/main.rs:25` exposes `run_scan` as a Tauri command without admin authentication check.
  - `/private/tmp/rocinante/src-tauri/src/git.rs:7` performs recursive repository discovery from caller-supplied root via `WalkDir`.
- Impact:
  - In a hostile front-end/plugin context, this enables broad local filesystem scanning and Git metadata collection.

3. Low — scoring weight integrity can be bypassed with unsigned payload
- Evidence:
  - `/private/tmp/rocinante/src-tauri/src/scoring.rs:25-44` accepts legacy plain JSON scoring weights as a fallback when signed envelope path fails.
- Impact:
  - Signature protection for configuration can be silently bypassed by replacing signed payloads with legacy format.

## Positive Findings
- SQL usage appears parameterized in inspected paths (`rusqlite`/DuckDB calls use bound params; no obvious raw string concatenation for user data).
- Git subprocess calls use argv arrays, not shell execution.
- No obvious front-end code sinks found for `dangerouslySetInnerHTML`, `innerHTML`, `eval`, or iframe injection during this pass.
- Existing test coverage in the target repo currently passes at baseline (`83` tests).

## Recommendations
1. Require `RUNICIPAL_TOKEN_SECRET` in production and fail fast when missing or weak; document it in `.env` examples.
2. Gate `run_scan` behind `require_admin` or an explicit allowlist policy for accepted root paths and traversal depth.
3. Remove permissive legacy scoring-weights parsing or isolate it to a controlled migration mode with explicit audit logging.

## Execution Metadata
- Analysis type: local code+test sweep (Tachi-Rust style)
- Timestamp marker: `report-2026-06-22T02-27-00Z.md`
- Repo path used for evidence: `/private/tmp/rocinante`
